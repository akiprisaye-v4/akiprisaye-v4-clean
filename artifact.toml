kind = "web"
previewPath = "/"
title = "AKI.HORIZON"
version = "1.0.0"
id = "artifacts/aki-horizon"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 24422

[services.development]
run = "pnpm --filter @workspace/aki-horizon run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/aki-horizon", "run", "build" ]
publicDir = "artifacts/aki-horizon/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "24422"
BASE_PATH = "/"
